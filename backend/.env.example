# Environment Variables for Food Ordering App

# Email Configuration (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Default restaurant email for notifications
DEFAULT_RESTAURANT_EMAIL=restaurant@example.com

# Database URL (if needed)
# MONGODB_URI=mongodb://localhost:27017/food-ordering-app

# JWT Secret (if needed)
# JWT_SECRET=your-jwt-secret

# Other environment variables
PORT=7000
NODE_ENV=development
